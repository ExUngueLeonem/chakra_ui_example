import { useEffect, useState } from "react";
const ApiFromServer = () => {
    const [data, setData] = useState(null);
    useEffect(() => {
        

    }, [])

    return (
        <div>
            ApiFromServer
        </div>
    );
};

export default ApiFromServer;